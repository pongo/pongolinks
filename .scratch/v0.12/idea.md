нужно спланировать v0.12. основная цель: добавление поиска и фильтрации.

сейчас в [BookmarkList.vue](apps/frontend/src/features/bookmarks/views/BookmarkListView/BookmarkList.vue) у каждой закладки указан домен, а затем идут теги:
<span class="break-all">{{ formatBookmarkDomain(bookmark.url) }}</span>
<span
              v-for="tag in bookmark.tags"
              :key="tag.id"
              class="ui-tag inline-flex max-w-full items-center border px-1.5 py-0.5 text-xs"
            >
{{ tag.name }}
</span>

по клику по домену должна происходить фильтрация по нему. в базе у ссылок нет отдельного столбца с доменом, но мы можем просто искать ссылки, начинающиеся с http://домен (и нужно учитывать оба протокола http и https). но я точно не знаю какой должен быть адрес у такой ссылки.

по клику по каждому должна происходить фильтрация по тегу (показать только закладки, имеющие этот тег). тоже не знаю как должна выглядеть адрес ссылки.

дальше. на странице с закладками сверху должно быть поле для поиска. и тут я совсем теряюсь. у нас в sqlite включен полно-текстовый поиск. и я хочу, чтобы у пользователя была возможность как фильтровать строго по тегам, так и искать более широко. и хотелось, чтобы -tag исключал бы этот тег из поиска.

не знаю, как это совместить в одной форме. один из вариантов: для фильтрации строго по тегу тег должен начинаться с #. а иначе полно-текстовый поиск (заголовок, ссылка, описание, теги).

еще если в поиск введена ссылка, то нужно ее искать (включая альтернативный протокол и related). наверное стоит использовать /search/check

кроме того, если пользователь введет /pongolinks/s/tag2 то будет поиск по тегу tag2 (не знаю, должен ли это быть поиск или фильтрация). еще не уверен что лучше использовать /s/ или /q/. кроме того, здесь должна быть возможность через пробел ввести несколько тегов

а в самом поле поиска должно работать автодополнение тегов (видимо нужно [BookmarkTagInput](apps/frontend/src/features/bookmarks/components/BookmarkTagInput/) перенести из среза bookmarks в более общий)
